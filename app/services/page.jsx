import Header from '../../components/Header';import Footer from '../../components/Footer';import site from '../../data/siteData.json';import Link from 'next/link';export const metadata={title:'Legal Services',description:'Complete list of Holland Legal Services pages for expats and entrepreneurs in the UAE.'};export default function Services(){return <><Header/><main className='serviceIndex pagePad'><div className='sectionHead'><p className='eyebrow'>Services</p><h1>Legal service pages</h1><p>Every topic has its own SEO page, content sections, expert cards, FAQs and enquiry form.</p></div><div className='cards'>{site.services.map(s=><Link className='card' key={s.slug} href={`/services/${s.slug}`}><p>{s.category}</p><h3>{s.title}</h3><span>{s.lead}</span><b>Read page →</b></Link>)}</div></main><Footer/></>}
