import site from '../data/siteData.json';export default function sitemap(){const base=site.baseUrl;return [{url:base,lastModified:new Date()},{url:`${base}/services`,lastModified:new Date()},{url:`${base}/insights`,lastModified:new Date()},{url:`${base}/contact`,lastModified:new Date()},...site.services.map(s=>({url:`${base}/services/${s.slug}`,lastModified:new Date()})),...site.insights.map(s=>({url:`${base}/insights/${s.slug}`,lastModified:new Date()}))]}
